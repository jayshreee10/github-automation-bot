# Neon Console → Auth → Auth URL (public, not a secret). Copy to .env.local.
VITE_NEON_AUTH_URL=https://ep-xxxx.neonauth.REGION.aws.neon.tech/neondb/auth

# github.com/apps/<slug> of the GitHub App (public, not a secret)
VITE_GITHUB_APP_SLUG=your-app-slug

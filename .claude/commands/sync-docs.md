---
description: Bring CLAUDE.md, AGENTS.md and README.md in line with the code
---

Make the context and project docs match the current code.

1. Read `CLAUDE.md`, `AGENTS.md`, `README.md` (if present), `package.json` scripts, `.env.example`, and the module folders under `backend/` and `frontend/`.
2. List mismatches: missing or outdated commands, modules, env vars, conventions, setup steps.
3. Propose concise edits. Keep `CLAUDE.md` for Claude-specific guidance and `AGENTS.md` for tool-neutral agent guidance; avoid duplicating content between them.
4. Apply after the user confirms.

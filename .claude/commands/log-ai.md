---
description: Record an AI decision or wrong turn in docs/ai-log.md
argument-hint: <what happened>
---

Add a note to `docs/ai-log.md` under the current session's heading (create the file or heading if missing, using the template in `docs/sessions.md`).

Note from the user: $ARGUMENTS

Place it under the best-fitting line: "Decisions I made", "Where the AI was wrong", or "Prompt worth keeping". For a wrong turn, capture: symptom → how it was noticed → fix. Keep the user's wording and do not embellish. Show the added lines when done.
